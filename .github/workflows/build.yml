name: Build x64+aarch64

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5
    - name: Set up Docker
      uses: docker/setup-docker-action@v5
    - name: Build
      run: ./run.sh
